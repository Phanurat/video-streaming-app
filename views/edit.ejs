<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= video.id ? "Edit Video" : "Add Video" %></title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-200 p-6">

<h1 class="text-2xl mb-4"><%= video.id ? "Edit Video" : "Add Video" %></h1>

<form action="<%= video.id ? '/videos/' + video.id : '/videos' %>" 
      method="POST" enctype="multipart/form-data" 
      class="flex flex-col gap-2">

  <label>Title:</label>
  <input type="text" name="title" value="<%= video.title || '' %>" 
         class="p-2 rounded bg-gray-700 text-white" required>

  <label>Video:</label>
  <input type="file" name="video" accept="video/*" <%= video.id ? '' : 'required' %> >

  <label>Thumbnail:</label>
  <input type="file" name="thumbnail" accept="image/*">

  <button type="submit" class="px-4 py-2 bg-cyan-600 rounded hover:bg-cyan-500 mt-2">
    <%= video.id ? 'Update Video' : 'Add Video' %>
  </button>
</form>

<a href="/" class="mt-4 inline-block text-cyan-400 hover:underline">Back to list</a>
</body>
</html>
